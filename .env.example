# ===========================================
# Defrag Environment Variables
# ===========================================

# ----- Application -----
NODE_ENV=development
PORT=3001

# ----- URLs -----
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001

# ----- CORS -----
# Comma-separated list of allowed origins (optional, defaults to FRONTEND_URL)
# CORS_ORIGINS=https://defrag.app,https://www.defrag.app

# ----- Database -----
DATABASE_URL=postgresql://defrag:defrag_dev_password@localhost:5432/defrag

# ----- Redis -----
# Option 1: Use REDIS_URL (recommended for production/managed Redis like Upstash)
REDIS_URL=redis://localhost:6379

# Option 2: Use individual settings (fallback if REDIS_URL not set)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Encryption (for OAuth tokens at rest)
ENCRYPTION_KEY=your-32-character-encryption-key-here

# OpenAI (for embeddings)
OPENAI_API_KEY=sk-your-openai-api-key

# ----- GitHub App -----
# Create at: https://github.com/settings/apps
GITHUB_APP_ID=your-github-app-id
GITHUB_APP_SLUG=your-github-app-slug
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END RSA PRIVATE KEY-----"
GITHUB_CALLBACK_URL=http://localhost:3001/api/connections/github/callback
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret

# ----- Slack OAuth -----
# Create at: https://api.slack.com/apps
SLACK_CLIENT_ID=your-slack-client-id
SLACK_CLIENT_SECRET=your-slack-client-secret
SLACK_SIGNING_SECRET=your-slack-signing-secret
SLACK_CALLBACK_URL=http://localhost:3001/api/connections/slack/callback

# ----- Notion OAuth -----
# Create at: https://www.notion.so/my-integrations
NOTION_CLIENT_ID=your-notion-client-id
NOTION_CLIENT_SECRET=your-notion-client-secret
NOTION_CALLBACK_URL=http://localhost:3001/api/connections/notion/callback

# ----- Google OAuth (Social Login) -----
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback

# ----- Kakao OAuth (Social Login) -----
# Create at: https://developers.kakao.com/console/app
KAKAO_CLIENT_ID=your-kakao-client-id
KAKAO_CLIENT_SECRET=your-kakao-client-secret
KAKAO_CALLBACK_URL=http://localhost:3001/api/auth/kakao/callback

# ----- Email (SMTP) -----
# For development: Use MailHog (docker-compose) with port 1025
# For production: Use real SMTP service (SendGrid, AWS SES, Gmail, etc.)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Defrag <noreply@defrag.app>
